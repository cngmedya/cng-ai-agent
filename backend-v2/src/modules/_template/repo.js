// _TEMPLATE REPO
// Bu modül için DB erişimi gerektiğinde burayı dolduracağız.
//
// Örnek kullanım:
//
// const db = require('../../core/db');
//
// function findExample(id) {
//   return db.prepare('SELECT * FROM some_table WHERE id = ?').get(id);
// }
//
// module.exports = {
//   findExample,
// };
